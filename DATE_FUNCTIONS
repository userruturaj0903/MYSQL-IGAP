use college;

SELECT * FROM STUDENT 
select CURDATE() , CURRENT_DATE(), CURRENT_TIME() , CURRENT_TIMESTAMP() , CURTIME() , LOCALTIME() , LOCALTIMESTAMP() , NOW() , SYSDATE();


SELECT DATE (NOW());

SELECT DAY (NOW());

SELECT DAYNAME (NOW());

SELECT DAYOFMONTH(NOW());

SELECT DAYOFWEEK(NOW());

SELECT DAYOFYEAR(NOW());

SELECT HOUR(NOW());

SELECT LAST_DAY(NOW());

SELECT microsecond(NOW());

SELECT MONTHNAME(NOW());

SELECT QUARTER(NOW());

SELECT SECOND(NOW());

SELECT TIME(NOW());

SELECT CURRENT_TIMESTAMP();

SELECT WEEK(NOW());

SELECT WEEKDAY(NOW());

SELECT WEEKOFYEAR(NOW());

SELECT YEAR(NOW());


SELECT ADDDATE(NOW() , INTERVAL 26 DAY);

SELECT ROUND(DATEDIFF(NOW() , '2001-03-09') / 365);


SELECT str_to_date('2001-03-09' , '%d - % M %Y');

SELECT DATE_FORMAT(NOW(), '%a');


SELECT STUDENT_ID , FIRST_NAME ,CASE WHEN FIRST_NAME = 'ROHAN' THEN 'YES' ELSE 'NO' END AS RESULT FROM STUDENT;


SELECT STUDENT_ID , FIRST_NAME , CASE WHEN FIRST_NAME = 'RAHUL' THEN ' YES ' ELSE 'NO' END AS RESULT FROM STUDENT;


SELECT STUDENT_ID , FIRST_NAME , CASE WHEN FIRST_NAME  = 'DURGA' THEN 'YES' ELSE 'NO' END AS RESULT FROM STUDENT;

SELECT CAST('25' AS SIGNED)AS NO;

SELECT CONVERT('25' , SIGNED)AS NO;

SELECT STUDENT_ID , FIRST_NAME , CASE WHEN FIRST_NAME = 'RAHUL' THEN 'YES' ELSE 'NO' END AS RESULT FROM STUDENT;


SELECT COALESCE(DDATE, '2001-03-09')
FROM STUDENT;

SELECT CURRENT_USER();

SELECT IF(500 < 1000 , 'YES' , 'NO');

SELECT IFNULL(NULL, 'IGAP');

SELECT ISNULL(NULL);

SELECT ISNULL('SL');

SELECT LAST_INSERT_ID();


 
